spring:
  profiles:
    active: test

---

spring:
  profiles: test
couchdb:
  client:
    url: http://localhost:5984/
    username: admin
    password: password
    bulk-max-size: 50
    find-execution-stats: true
